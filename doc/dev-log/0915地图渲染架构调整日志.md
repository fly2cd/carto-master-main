# 地图渲染架构调整日志

## 文档信息

| 项目 | 内容 |
|---|---|
| 日期 | 2026-09-15 |
| 主题 | 将生产适配从 QGIS 调整为 MapLibre + SVG 前端渲染执行器，输出可编辑 SVG |
| 涉及阶段 | U-P1（内核骨架与协议层）后续调整 |
| 依据 | [实施计划 v2.1](../implement-plan.md)、[U-P1 说明文档](../impl-log/U-P1说明文档.md) |
| 状态 | 最终调整建议，待实施 |
| 版本 | v2（融入 PPT Master 对比、输出格式决策、后端渲染服务分析） |

---

## 调整演进脉络

本文档经历了三轮迭代：

| 轮次 | 核心命题 | 结论 |
|---|---|---|
| 第一轮 | 将 QGIS 适配器替换为 MapLibre + Canvas 前端渲染器 | 建立稳定的 RenderScene 合同，后端做决策，前端做呈现 |
| 第二轮 | 对比 PPT Master 架构，确认是否需要后端渲染 | 地图必须栅格化，但不需要自研后端渲染引擎；需要受控无头浏览器渲染服务 |
| 第三轮 | 如果产物需要要素级编辑，应该用什么格式 | overlay 用 SVG 矢量元素，产物为可编辑 SVG，轻量合成器拼接 |

三轮迭代的最终结论：**不是"把 QGIS 换成 MapLibre"，而是建立 RenderScene → 无头浏览器渲染 → SVG 合成 → 可编辑产物的完整确定性管线。**

---

## 一、核心架构调整

### 1. 将前端页面定位为 Executor

当前的 Strategy/Executor 两层仍然成立：

| 层次 | 主要职责 |
|---|---|
| Strategy | 理解制图意图、选择模板、绑定数据、确定分类方法、范围、符号语义和版面 |
| Compiler | 将地图计划编译成确定性的 RenderScene |
| Frontend Executor | 使用 MapLibre 和 SVG 还原 RenderScene |
| Validator | 检查语义、资源、渲染效果和输出成果 |
| SVG Compositor | 将地图截图与 SVG overlay 合成为可编辑 SVG 产物 |

前端执行器不能自主决定：

- 分类字段和分类断点；
- 哪些数据进入地图；
- 数据值与符号的映射关系；
- 图层是否代表观测、预测或推断数据；
- 模板硬规则是否可以忽略；
- 地图范围、输出尺寸和正式成果参数。

它只负责忠实执行已编译的场景。

### 2. 不再单独建设 Layout Compositor，改为 SVG 合成器

原建议中存在一个独立的 `layout_compositor.py`。经分析，版面合成应在前端通过 SVG overlay 元素完成，后端只需要一个轻量合成器将地图截图与 overlay 拼接为最终 SVG。

```
（原方案）
MapLibreRenderer → LayoutCompositor → PNG/PDF

（调整后）
前端渲染执行器
├─ MapLibreHost           ← WebGL 地图渲染（必然栅格）
├─ SvgOverlayEngine       ← SVG/HTML 矢量叠加元素（图名/图例/指北针等）
├─ SceneCoordinator       ← 协调两者刷新与投影
└─ ExportController       ← 预览截图 + 正式导出
         ↓
后端 SVG 合成器（~300 行）
├─ 嵌入地图截图为 <image>
├─ 追加 SVG overlay 元素（保持矢量）
└─ 内联字体/图标资源
         ↓
可编辑 SVG 产物
```

### 3. 适配器命名

推荐使用：**`WebMapRendererAdapter`**

实现标识记录为：

```
renderer_id: maplibre-web
```

这样未来即使 MapLibre 版本或前端框架改变，内部协议也不需要重新命名。

> **命名修正**：原建议中"CanvasOverlayEngine"应改为"SvgOverlayEngine"。overlay 必须用 SVG/HTML 元素实现而非 `<canvas>` 位图绘制，这是从"终端栅格产物"升级为"可编辑矢量产物"的唯一前提。

---

## 二、需要新增统一的 RenderScene 协议

后端不能直接向前端发送零散的 MapLibre 配置和绘图命令。建议编译成一个统一的、不可执行的类型化场景协议。

```json
{
  "schema_version": 1,
  "scene_id": "scene-flood-001",
  "renderer_id": "maplibre-web",
  "viewport": {
    "width_px": 1684,
    "height_px": 1191,
    "device_pixel_ratio": 2,
    "background": "#FFFFFF"
  },
  "camera": {
    "bounds": [112.1, 28.0, 114.2, 29.8],
    "bearing": 0,
    "pitch": 0,
    "padding": { "top": 80, "right": 320, "bottom": 90, "left": 80 }
  },
  "map": {
    "style_ref": { "id": "flood-style", "version": "1.0.0", "digest": "sha256:..." },
    "sources": [],
    "layers": []
  },
  "overlays": [
    {
      "id": "map-title",
      "type": "text",
      "coordinate_space": "page",
      "position": { "x": 0.05, "y": 0.04 },
      "content": "洪涝灾害风险专题图",
      "style_role": "map-title"
    },
    {
      "id": "survey-point-01",
      "type": "symbol",
      "coordinate_space": "geographic",
      "anchor": { "longitude": 113.1, "latitude": 28.7 },
      "symbol_ref": "survey-location",
      "label": "调研点一"
    }
  ],
  "export": {
    "targets": ["svg", "pdf", "png"],
    "dpi": 300
  }
}
```

> **关键修正**：`export.targets` 增加 `svg` 作为首要产物格式。`svg` = 可编辑矢量产物，`pdf` = 从 SVG 转换的混合质量文档，`png` = 纯栅格预览图。

这个协议应该成为前后端唯一的地图渲染合同。

> **Schema 约束**：RenderScene 的字段要严格用 `additionalProperties: false`，overlay 类型用 `enum` 限定（text/symbol/legend/north-arrow/scale-bar/attribution/complex-symbol），不要留开放式 `type: string`。

---

## 三、必须区分四种坐标空间

因为 overlay 同时承载版面元素和地图专题符号，所以坐标系统是这套架构最关键的部分。

建议每个 overlay 元素必须声明 `coordinate_space`：

| 坐标空间 | 用途 | 示例 |
|---|---|---|
| `page` | 相对整张制图页面固定 | 图名、数据来源、Logo、页码 |
| `viewport` | 相对地图视口固定 | 图例、比例尺、指北针 |
| `geographic` | 随地图缩放和平移 | 调研点、灾害点、线路箭头、专题标记 |
| `feature` | 绑定到某个地图要素 | 行政区名称、重点企业标记、设施标注 |

其中：

- `page` 建议采用 0～1 的归一化坐标。
- `viewport` 可采用像素或归一化坐标。
- `geographic` 使用经纬度或规范化坐标。
- `feature` 使用 `source_id` + `feature_id` + `anchor_rule`。

不能只保存最终屏幕上的 x/y。否则地图缩放、平移或改变输出尺寸后，专题符号会与地图错位。

---

## 四、Overlay 技术选型：SVG 而非 Canvas 位图

这是第二轮分析中最重要的修正。

### 问题

原建议中 overlay 引擎命名为"CanvasOverlayEngine"，暗示用 `<canvas>` 2D context 绘制标题、图例、指北针等。但 `<canvas>` 的内容本质上是一张位图——无论怎么导出都是栅格的。

### 影响

| overlay 实现方式 | Chrome headless `page.pdf()` 输出 | 文字质量 | 要素可编辑性 |
|---|---|---|---|
| `<canvas>` 2D context | overlay 是栅格 | 放大有锯齿 | 不可单独编辑 |
| SVG / HTML 元素 | overlay 文字是**矢量** | 无限缩放不失真 | 每个元素可单独选中编辑 |

### 决策

overlay 必须用 **SVG / HTML DOM 元素**实现，不能用 `<canvas>` 位图绘制。这样：

1. **Chrome headless `page.pdf()` 自动产出混合质量 PDF**：地图栅格 + overlay 矢量文字
2. **SVG 合成器可提取 overlay 元素**：overlay 在 DOM 中是 SVG 节点，直接拷贝到产物 SVG
3. **要素级编辑**：每个 overlay 元素是独立 DOM 节点，有 `id`，可被浏览器/Inkscape/Illustrator 选中编辑

```
前端渲染执行器
├─ MapLibreHost          → <canvas> (WebGL 地图，必然栅格)
├─ SvgOverlayEngine     → SVG/HTML 元素 (标题/图例/指北针，矢量)
├─ SceneCoordinator     → 协调两者
└─ ExportController
    ├─ 预览：截图
    └─ 正式：headless Chrome 渲染 → 地图截图 + SVG overlay → 合成器
```

---

## 五、前端需要增加 SceneCoordinator

MapLibre 和 overlay 不能各自独立刷新，需要有一个统一协调器：

```
SceneCoordinator
├─ 加载 RenderScene
├─ 初始化 MapLibre 地图
├─ 等待底图和专题图层稳定
├─ 获取统一相机和视口状态
├─ 投影 geographic/feature 锚点到屏幕坐标
├─ 更新 SVG overlay 元素的 transform 属性
├─ 检查字体、图标和瓦片资源
└─ 通知 ExportController 可以导出
```

需要处理的前端事件包括：

- 地图初始化完成；style 加载完成；source 数据完成；
- 字体和图标加载完成；
- 地图平移、缩放、旋转；页面 resize；
- 地图进入稳定状态；导出尺寸或 DPI 改变。

对地理锚定的 overlay 元素，每次地图相机变化时都需要重新计算屏幕位置并更新 SVG 元素的 `transform` 属性。

> **后端边界说明**：SceneCoordinator 是纯前端实现，后端不需要知道其存在——后端只发 RenderScene、收 RenderReceipt。SceneCoordinator 属于前端工程范畴，不进 Schema 和策略文件。

---

## 六、明确 MapLibre 和 Overlay 的职责边界

### MapLibre 负责

- 底图；行政区面；河流、道路等连续线要素；
- 面分级设色；普通点、线、面专题图层；
- 需要随缩放裁切的图层；
- 地图范围、缩放、旋转和视口；
- 可交互查询的业务要素。

### SVG Overlay 负责

- 图名、副标题；图例；指北针；比例尺；
- 数据来源和署名；页边信息；
- 自定义复杂符号；调研点标记；
- 路线装饰和特殊箭头；领导批示、重点标注和引线；
- 需要精确页面布局的注记；
- MapLibre 样式难以表达的复合专题符号。

不能让同一个语义图层同时由 MapLibre 和 overlay 分别决定样式。例如"洪涝风险等级"如果由 MapLibre 绘制面分级设色，图例必须读取同一份编译后的分类和颜色定义，不能在 overlay 中重新计算。

---

## 七、模板系统需要怎样调整

现有四类模板不需要增加第五类。

### MapBrand

同时输出 MapLibre 和 SVG overlay 共用的身份 Token：

- `color.primary` / `color.accent`
- `font.title` / `font.label` / `font.legend`
- `logo.asset`
- `attribution.style`

### MapStyle

定义制图语义和符号映射，但不直接保存任意 JavaScript：

- 分类方法；颜色映射；图层顺序；标注规则；
- 点线面表达；SVG overlay 符号角色；MapLibre layer 类型映射。

### MapLayout

主要由 SVG overlay 消费：

- 页面尺寸；地图视口区域；标题区域；图例区域；
- 指北针位置；比例尺位置；数据来源位置；安全边距；附图区域。

已有的 frames 继续使用，但增加：

- `coordinate_space` / `z_index` / `clip` / `responsive_behavior` / `visibility_condition`

### MapScenario

负责组合：MapLibre 图层集合；SVG overlay 元素集合；数据角色；页面结构；输出目标；场景特有规则。

---

## 八、协议层建议只新增两个主要协议

### 1. `render-scene.schema.json`

描述前端需要渲染的完整确定性场景：viewport；camera；MapLibre style/source/layer；SVG overlay；坐标空间；资源引用；输出参数（含 svg 目标）；环境要求；场景摘要。

### 2. `render-receipt.schema.json`

前端完成渲染后返回：`receipt_id` / `session_id` / `scene_id` / `scene_revision` / `scene_digest` / `renderer_id` / `renderer_profile_digest` / 各组件版本 / `status` / `viewport` / `resource_evidence` / `output_artifacts` / `warnings` / 起止时间 / `attestation_algorithm` / `renderer_attestation`。

> `renderer_attestation` 使用至少 32 字节密钥生成 HMAC-SHA256，绑定整个回执内容；后端还会将回执与已提交会话、Scene 摘要、修订号、Renderer Profile 摘要、组件版本、资源摘要和输出目标逐项核对。

不需要为标题、图例、指北针分别创建独立协议。它们作为 `render-scene.overlays[]` 的类型化节点即可。

---

## 九、现有执行快照需要调整

`common.schema.json` 中的 `executionSnapshot` 建议增加：`render_scene_ref` / `renderer_profile_ref` / `camera` / `overlay_coordinate_policy` / `interaction_policy`。

`environment` 建议从：

```json
{ "adapter_id": "...", "adapter_version": "...", "fingerprint": "..." }
```

扩充为：

```json
{
  "renderer_id": "maplibre-web",
  "renderer_version": "1.0.0",
  "frontend_build": "2026.09.15",
  "maplibre_version": "...",
  "overlay_engine_version": "...",
  "fingerprint": "sha256:..."
}
```

锁中只存摘要引用，不存完整浏览器环境。具体浏览器版本等运行信息放在 `render-receipt` 中。

---

## 十、适配器层需要怎样调整

原来的 `QgisEnvironmentProbe` / `QGISAdapter` / `Renderer` 建议改成：

```
WebMapRendererAdapter
RendererCapabilityRegistry
RenderSessionGateway
RenderReceiptValidator
SvgCompositor（轻量合成器）
```

`WebMapRendererAdapter` 不负责实际绘图，而负责：

1. 校验前端渲染器是否已注册；
2. 校验其版本和能力；
3. 下发 RenderScene；
4. 接收 RenderReceipt；
5. 校验场景摘要是否一致；
6. 触发 SVG 合成器生成最终产物；
7. 将回执写入步骤记录。

前端页面是实际执行器，后端 Adapter 是可信边界和协议桥梁。

---

## 十一、环境探测应改为前端能力握手

现有 `qgis_probe.py` 不应简单改成检查本机是否安装 MapLibre。

因为 MapLibre 实际运行在前端页面中，后端本地环境探测不能代表用户浏览器。

更合适的是前端启动后提交：

```json
{
  "renderer_id": "maplibre-web",
  "frontend_build": "2026.09.15",
  "maplibre_version": "...",
  "overlay_engine_version": "...",
  "browser_engine": "...",
  "webgl_available": true,
  "supported_exports": ["svg", "png"],
  "max_canvas_size": { "width": 16384, "height": 16384 },
  "device_pixel_ratio": 2,
  "available_fonts": ["Microsoft YaHei"],
  "offline_rendering": true
}
```

后端校验后形成环境指纹。如果未来部署受控的无头导出服务，也使用完全相同的能力握手协议。

> **安全约束**：前端提交的能力清单是自报的，不能直接信任——后端需要对其做范围校验（DPR 不能超过 4，max_canvas_size 不能超过已知上限）。

---

## 十二、预览和正式出图要分开

### 浏览器交互预览

用于：查看地图；调整范围；拖动图例；调整专题标注；查看候选效果；形成 G2 审批前预览。

浏览器中的人工调整不能直接修改冻结结果。调整应生成类型化变更：`move-overlay` / `resize-overlay` / `change-camera` / `toggle-layer` / `update-label-anchor`。这些变更回传后端，产生新的候选版本。

### 受控正式输出（必须，非可选）

企业级生产**必须**使用受控无头浏览器，这不是可选项：

```
同一个前端渲染包
    ↓
受控无头 Chrome 加载
    ↓
读取冻结后的 RenderScene
    ↓
渲染地图（WebGL 截图）+ 渲染 overlay（SVG DOM）
    ↓
SVG 合成器拼接
    ↓
可编辑 SVG 产物
```

**为什么必须用无头浏览器而非用户浏览器**：

| | 用户浏览器（预览） | 受控无头 Chrome（正式） |
|---|---|---|
| 浏览器版本 | 不可控 | 固定版本 |
| DPR | 不可控（1~3） | 固定值 |
| 字体 | 不可控 | 固定字体集 |
| WebGL 实现 | 不可控 | 固定 |
| 可信度 | 不可信（可被篡改） | 可信 |
| 可重放性 | 不保证 | 保证（同 RenderScene → 同像素） |

当前 `MapSpecLock` 的 `execution_digest` 机制要求冻结后输入不变，确保可重放。但如果渲染发生在用户浏览器，相同输入不保证相同输出。只有受控无头浏览器能保证确定性。

---

## 十三、输出格式与合成策略

### 产物格式决策

| 格式 | 定位 | 地图部分 | overlay 部分 | 要素可编辑 |
|---|---|---|---|---|
| **SVG** | **首要产物** | 嵌入为 `<image>`（栅格） | SVG 矢量元素 | 是 |
| PDF | 从 SVG 转换 | 栅格 | 矢量文字 | 需 Illustrator |
| PNG | 纯预览 | 栅格 | 栅格 | 否 |

**SVG 作为首要产物的理由**：

1. overlay 元素本身就是 SVG，合成器只需拼接，无需格式转换
2. 每个元素有 `id`，可被浏览器/Inkscape/Illustrator 选中编辑
3. 地图画面嵌入为 `<image>`（栅格），不可编辑但也不需要编辑——改地图应回后端重新编译
4. SVG → PDF/PNG 是简单转换，不需要写编译器

### SVG 合成器（后端，约 300 行）

合成器逻辑本质上是拼接 + 资源内联：

```python
def compose_svg(render_scene, map_screenshot_path, overlay_elements) -> str:
    # 1. 地图截图嵌入为 <image>
    # 2. overlay 元素已经是 SVG，直接追加（保持矢量）
    # 3. 内联字体和图标资源
    # 4. 输出自包含 SVG
```

对比 PPT Master 的 svg_to_pptx 编译器（13000+ 行），Carto Agent 的合成器极轻，因为：

| | PPT Master svg_to_pptx | Carto Agent SVG 合成器 |
|---|---|---|
| 做什么 | SVG → DrawingML 跨格式转换 | SVG 片段 + 栅格图 → 单个 SVG 文件 |
| 元素映射 | 5322 行（每个标签 → DrawingML 对象） | 零（SVG 元素就是 SVG 元素） |
| 复杂度 | 高（跨格式、跨对象模型） | 低（同格式拼接 + 资源内联） |

---

## 十四、后端渲染的三个层次

| 类型 | 含义 | 是否需要 | 理由 |
|---|---|---|---|
| **A. 后端渲染引擎** | 自己写代码画像素 | 不需要 | MapLibre 是成熟引擎，重写等于重写 MapLibre |
| **B. 后端渲染服务** | 受控环境跑同一个前端引擎 | **必须** | 可重放性、可信度、批量生成的硬要求 |
| **C. 后端格式编译器** | 把中间语言转成目标格式 | 轻量版需要 | SVG 合成器（~300 行），非 PPT Master 式重编译器 |

PPT Master 只需要 C（格式编译器），不需要 B（渲染服务），因为 PPTX 是编译出来的，不是渲染出来的。

Carto Agent 的地图必须渲染（WebGL 像素），所以需要 B（无头浏览器渲染服务）。但因为目标是 SVG 而非 PPTX，所以 C 是轻量合成器而非重编译器。

---

## 十五、验证体系需要增加的检查

### 场景静态校验

- 所有 MapLibre source 和 layer ID 唯一；
- SVG overlay 节点引用的符号和字体存在；
- 图例读取的分类、颜色和符号与专题图层一致；
- 地理锚点合法；
- 页面元素没有越过安全区；
- 图层和 overlay 的 z-index 没有冲突；
- 不允许任意 JavaScript 绘图代码。

### 前端预检

- MapLibre style 已加载；source、瓦片、sprite、glyph 已加载；
- SVG overlay 字体和图标已加载；
- 所有地理符号已成功投影；
- 无缺失资源；输出尺寸受支持；前端版本与场景要求匹配。

### 产物校验

- SVG 产物中每个 overlay 元素有唯一 `id`；
- 地图 `<image>` 的 digest 与渲染回执一致；
- 内联资源（字体、图标）完整无缺失；
- SVG 可被标准解析器（`xml.etree`）无错打开。

---

## 十六、ToolGateway 能力建议

```yaml
- capability_id: renderer-capability-register
  effect: read

- capability_id: web-map-preview
  effect: transform

- capability_id: web-map-export      # 无头浏览器渲染 + SVG 合成
  effect: write-project

- capability_id: svg-compose          # SVG 合成器
  effect: write-project

implementation: maplibre-web
```

业务工作流只依赖 `web-map-preview` / `web-map-export` / `svg-compose`，不直接依赖某个 MapLibre 类或前端框架。

---

## 十七、不需要调整的部分

以下架构可以保留：

四个顶层路由；五模块结构；Strategy/Executor 分层；IntentResolver；MapPlanner；MapComposer；DataPreparationService；DomainKnowledgeService；ToolGateway；G1/G2/G3 审批；ResolvedMap；MapSpecLock；不可变仓库；六级规则强度；四类模板；MCP 发现不等于授权；单智能体主架构。

前端执行器是确定性工具，不是一个新的智能体。

---

## 十八、PPT Master 对比与借鉴

### 架构对比

| | PPT Master | Carto Agent 调整方案 |
|---|---|---|
| 中间语言 | SVG（W3C 标准） | RenderScene JSON（自定义协议） |
| 前端渲染 | 浏览器原生 SVG 渲染 | MapLibre + SVG overlay |
| 后端编译器 | svg_to_pptx（13000+ 行，跨格式） | SVG 合成器（~300 行，同格式拼接） |
| 后端渲染服务 | 不需要（PPTX 是编译的） | 需要（地图必须渲染，无头浏览器） |
| 最终产物 | .pptx（矢量 DrawingML，可编辑） | .svg（矢量 overlay + 栅格地图，可编辑） |
| 前端预览 | 辅助、可选 | 主要预览方式 |
| 正式输出来源 | 后端编译器（权威） | 后端无头浏览器 + SVG 合成器 |

### 可借鉴的 5 个设计模式

1. **前端标注 → 类型化变更 → 回传后端的闭环**：PPT Master 的 annotation 系统验证了此模式。Carto Agent 的 `move-overlay` 等变更回传是同一模式的升级版（从自由文本到结构化变更）。

2. **预览 ≠ 正式输出的明确边界**：PPT Master 硬规则"the editor never runs the export pipeline"。Carto Agent 应在策略文件中写死"前端不执行正式导出"。

3. **SVG 作为中间语言**：PPT Master 证明 SVG 既能浏览器原生渲染，又能后端解析编译。Carto Agent 的 overlay 直接用 SVG 获得了同样的双重优势。

4. **finalize 步骤**：PPT Master 的 `finalize_svg.py` 生成自包含 SVG。Carto Agent 的 SVG 合成器也应有 finalize 步骤，确保所有资源内联。

5. **编译器 = 确定性管道**：PPT Master 的 svg_to_pptx 是纯确定性的，没有模型参与。Carto Agent 的 RenderSceneCompiler 和 SVG 合成器也应如此。

### 根本差异：目标格式

PPT Master → 目标是 PPTX（DrawingML XML），必须写编译器把 SVG 转成 DrawingML。

Carto Agent → 目标是 SVG（本身就是中间语言），不需要跨格式编译。Chrome 自带 PDF 引擎，SVG → PDF 是简单转换。

**所以 Carto Agent 不需要 PPT Master 那样的 13000 行后端编译器。它需要的是无头浏览器渲染服务 + 轻量 SVG 合成器。**

---

## 最终架构

```
用户请求
   ↓
IntentResolver
   ↓
MapPlanner
   ↓
DataPreparationService
   ↓
MapComposer
   ↓
ProtocolCompiler
   ↓
ResolvedMap
   ↓
RenderSceneCompiler（确定性，不调模型）
   ↓
render-scene.json
   ↓
WebMapRendererAdapter
   ↓
┌─────────────────────────────────────┐
│ 前端地图渲染执行器                      │
│  ├─ MapLibreHost (WebGL 地图)        │
│  ├─ SvgOverlayEngine (矢量叠加)      │
│  ├─ SceneCoordinator (协调)          │
│  └─ ExportController                │
└─────────────────────────────────────┘
   ↓
render-receipt.json + 地图截图 + SVG overlay 元素
   ↓
Validator
   ↓
G2 冻结
   ↓
MapSpecLock
   ↓
┌─────────────────────────────────────┐
│ 受控无头 Chrome（Type B 渲染服务）     │
│  加载同一份前端渲染包                   │
│  读取冻结后的 RenderScene              │
│  渲染地图截图 + 提取 SVG overlay      │
└─────────────────────────────────────┘
   ↓
SVG 合成器（Type C-lite，~300 行）
   ├─ 嵌入地图截图为 <image>
   ├─ 追加 SVG overlay 元素（保持矢量）
   └─ 内联字体/图标资源
   ↓
可编辑 SVG 产物
   ├─ 可在浏览器/Inkscape/Illustrator 中编辑要素
   ├─ 可转换为 PDF（矢量文字 + 栅格地图）
   └─ 可转换为 PNG（纯栅格预览）
```

---

## 评估总结

| 维度 | 评分 | 说明 |
|---|---|---|
| 架构兼容性 | 高 | 不改五模块、不改安全底座、不改四类模板、不改审批门禁。新增的 RenderScene、render-receipt 和 SVG 合成器是现有协议体系上的自然延伸。 |
| 改动控制力 | 高 | 后端改动集中在 adapters 层 + 2 个新 Schema + environment-fingerprint 泛化 + SVG 合成器（~300 行）。核心模块零改动。 |
| 安全一致性 | 高 | 正式输出走受控无头浏览器，确保可重放。RenderReceipt 使用 HMAC-SHA256 绑定会话、Scene、Renderer Profile、资源和输出证据。SVG 合成器在后端可信环境运行并强制路径允许根。 |
| 产物质量 | 高 | SVG overlay 矢量化解决了原方案"栅格 PDF 质量降级"风险。文字无限缩放不失真，每个 overlay 元素可单独编辑。 |
| 可演进性 | 高 | `renderer_id: maplibre-web` 明确当前实现是 MapLibre GL JS Web 渲染器；未来换引擎时通过新的受信 Renderer Profile 和能力协商接入。SVG 产物格式是开放标准。 |
| 实现复杂度 | 中 | SceneCoordinator、SVG overlay 投影、无头浏览器服务是前端工程复杂度。后端 SVG 合成器极轻（~300 行）。 |

**总体判断：建议合理，可采纳。** 落地时的关键约束是：

1. overlay 必须用 SVG/HTML 元素，不能用 `<canvas>` 位图——这是矢量产物和栅格产物的分水岭；
2. 正式输出必须走受控无头浏览器——这是可重放性和可信度的硬要求；
3. RenderScene Schema 严格封闭（overlay 类型用 enum、`additionalProperties: false`）；
4. SVG 合成器在后端运行，产出自包含 SVG，再按需转 PDF/PNG。

---

## 常见问题（FAQ）

### 1. RenderScene 是唯一的后端到前端协议吗？

`RenderScene` 应当是后端向前端传递**可视化场景状态的唯一权威协议**，但不应承担前后端之间的全部通信职责。

前端拿到合法、完整且资源可访问的 `RenderScene` 后，应能够独立完成 MapLibre 地图层和 SVG overlay 的可视化，不再直接读取 MapScenario、MapMethod、用户原始意图、领域知识库、MCP 工具结果、`ResolvedMap` 或后端业务实体。

完整系统仍需保留职责单一的辅助协议：

| 方向 | 协议 | 职责 |
|---|---|---|
| 后端 → 前端 | `RendererCapabilities` | 渲染器版本和能力握手 |
| 后端 → 前端 | `RenderScene` | 唯一的场景描述和渲染状态协议 |
| 后端 → 前端 | `ResourceResponse` | 返回受控字体、图标、数据、瓦片等资源 |
| 前端 → 后端 | `EditOperation` | 回传移动、缩放、修改相机等类型化编辑操作 |
| 前端 → 后端 | `RenderStatus` | 报告资源加载和场景执行状态 |
| 前端/渲染服务 → 后端 | `RenderReceipt` | 提交可审计的渲染结果和环境证明 |

因此，更准确的约定是：`RenderScene` 是唯一的渲染状态协议，`EditOperation` 是唯一的编辑命令协议，`RenderReceipt` 是唯一的渲染结果证明协议。

### 2. 前端只通过 RenderScene 就可以完成可视化吗？

原则上可以，但必须满足三个条件：

1. **场景自足**：RenderScene 可以不内嵌大型数据和二进制资源，但必须包含完整的受控资源引用、资源类型、摘要和必需性声明。前端不能自行猜测地址或访问任意外部 URL。
2. **能力匹配**：RenderScene 必须声明 MapLibre、SVG、WebGL、地理锚定、字体等最低能力要求；前端通过 `RendererCapabilities` 声明实际能力。不满足必需能力时必须明确拒绝或受控降级，不能静默忽略。
3. **没有业务推理缺口**：分级、配色、符号语义、图例映射和可见性规则必须由后端确定。前端只执行确定性表达，不能收到类似 `classification: appropriate` 这种需要再次判断的模糊指令。

资源引用示例：

```yaml
resources:
  - resource_id: admin-boundary
    kind: geojson
    uri: resource://scene-001/admin-boundary
    digest: sha256:...
    required: true
renderer_requirements:
  map_engine: maplibre
  overlay_engine: svg
  webgl: true
  geographic_overlay: true
  minimum_protocol_version: 1.0
```

### 3. RenderScene 在系统中的边界是什么？

```text
后端业务模型
    ↓
ResolvedMap
    ↓
RenderSceneCompiler       隔离业务模型和模板变化
    ↓
RenderScene vN            唯一共享场景契约
    ├─ Web 前端渲染器
    ├─ 受控无头浏览器渲染器
    ├─ Render Validator
    └─ SVG Composer
```

MapScenario、MapMethod、知识库、MCP 工具和数据处理流程可以持续演进。只要 `RenderSceneCompiler` 仍输出兼容的 RenderScene，前端就不需要同步修改。

### 4. RenderScene 是否可以继续修改？

可以，而且在进入前后端大规模并行开发前，应继续收敛协议细节。但必须将协议划分为“稳定核心”和“可扩展能力”。建议的顶层结构为：

```yaml
schema: carto.render-scene
schema_version: 1.0
scene_id: scene-001
revision: 13
digest: sha256:...
viewport: {}
spatial_context: {}
camera: {}
tokens: {}
map: {}
overlays: []
resources: []
interactions: {}
export_targets: []
renderer_requirements: {}
provenance: {}
```

应尽早稳定的核心包括：

1. `scene_id`、`revision`、`schema_version` 和摘要计算范围；
2. viewport 的单位、坐标原点和旋转方向；
3. CRS、经纬度顺序和 camera 表达；
4. `source → layer` 的引用关系；
5. `page`、`viewport`、`geographic`、`feature` 四种坐标空间及转换规则；
6. overlay ID、地图符号和图例项的关联方式；
7. MapLibre、SVG overlay 和图例共用的符号 Token；
8. 资源 URI、快照、摘要和缓存规则；
9. 能力缺失时的错误和降级语义。

可以持续扩展新的封闭 overlay 类型、MapLibre layer 类型、图例布局、专题符号、交互操作、导出目标和资源类型。扩展必须使用明确的 `enum`、`oneOf` 和子 Schema，不能依赖 `additionalProperties: true` 或任意脚本。

### 5. 修改 RenderScene 后，整体代码改动大吗？

取决于修改类型。RenderScene 不是零耦合对象，而是前后端之间有意建立的、版本化且可控制的契约耦合。

| 修改等级 | 典型修改 | 预期影响 |
|---|---|---|
| 一级：兼容性扩展 | 新增具有默认值的可选字段 | 小。修改 Schema、数据类型、局部渲染组件和契约测试 |
| 二级：新增封闭能力 | 新增一种 overlay、layer 或导出类型 | 中。涉及编译器、前端注册表、SVG 渲染、验证器和 Fixture |
| 三级：改变既有语义 | 改变坐标含义、默认单位、资源解析或锚定规则 | 大。影响编译、预览、编辑、重放、合成、锁和历史数据 |
| 四级：重构核心结构 | 取消坐标空间、重构 source/layer 模型或改变场景生命周期 | 很大。需要新主版本、迁移器和前后端双版本支持 |

例如，新增可选 `opacity` 通常是一级修改；新增 `statistical-chart` overlay 通常是二级修改；把 `position.x/y` 从页面坐标改成地图视口坐标则属于三级破坏性修改。

### 6. RenderScene 与前端是完全解耦的吗？

不是，也不应追求完全解耦。正确目标是将耦合限制在稳定契约上：

```text
业务层与前端                  高度解耦
模板系统与前端                高度解耦
MCP、知识库与前端             完全隔离
RenderSceneCompiler 与前端    通过 RenderScene 契约耦合
RenderScene 与所有消费者       版本化、可测试的受控耦合
```

如果 RenderScene 的核心语义发生破坏性变化，Web 前端、无头浏览器、Validator 和 SVG Composer 都需要适配，这是正常且必要的协议演进成本。

### 7. 应如何控制协议版本和兼容性？

建议采用显式版本和能力协商：

```yaml
schema: carto.render-scene
schema_version: 1.1
minimum_renderer_version: 1.0
```

版本规则：

| 修改类型 | 版本策略 |
|---|---|
| 文字澄清，不改变结构和语义 | 不升级 |
| 新增可选字段或向后兼容能力 | `1.0 → 1.1` |
| 删除字段或改变既有字段语义 | `1.x → 2.0` |

后端建议支持当前主版本和上一个主版本，并根据 `RendererCapabilities` 选择双方都支持的最高协议版本。破坏性主版本迁移可以在 `rendering/migrations/` 中提供确定性迁移器，不应为每个小版本复制一套并行实现。

### 8. 所有资源都需要放进一个 RenderScene JSON 吗？

不需要。“唯一场景协议”不等于“单个巨大 JSON 文件”。大型 GeoJSON、字体、图标、图片和瓦片应通过摘要化的受控资源引用加载。离线重放和正式输出可以使用不可变 `RenderBundle`：

```text
RenderBundle
├── render-scene.json
├── resource-manifest.json
└── resources/
    ├── data/
    ├── fonts/
    ├── icons/
    ├── sprites/
    └── images/
```

其中，RenderScene 是逻辑协议和唯一场景真相；`resource-manifest.json` 描述资源摘要、类型和路径；RenderBundle 是传输、缓存、离线重放和正式渲染的载体。

### 9. 用户在前端调整地图时，是否直接回传整个 RenderScene？

不建议。前端应回传类型化 `EditOperation`，由后端校验并生成新的场景修订版本。例如：

```yaml
operation_id: op-1001
operation: move-overlay
scene_id: scene-001
base_revision: 12
target_id: legend-main
position:
  coordinate_space: page
  x: 1280
  y: 720
```

```text
RenderScene revision 12
→ 验证并应用 move-overlay
→ RenderScene revision 13
```

这样可以支持字段级校验、撤销重做、并发冲突检测、审计记录、权限控制和 `MapSpecLock`。

### 10. 当前阶段最应优先冻结哪些协议决策？

在实现更多专题场景之前，应优先冻结以下五项：

1. 四种坐标空间及其转换规则；
2. MapLibre layer 与 SVG overlay 的职责边界；
3. MapLibre、SVG overlay 和图例共用的符号 Token；
4. 资源引用、快照、摘要和 RenderBundle 规则；
5. Schema 版本、渲染器能力协商和错误降级机制。

只要这五项保持稳定，后续增加政务专题符号、地质滑坡、洪涝灾害、森林火灾、领导调研点位、线路标记和新图例类型，通常都属于局部扩展，不需要重构整个前后端。
